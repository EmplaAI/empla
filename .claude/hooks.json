{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); sys.exit(2) if ('.env' in f and '.example' not in f and '.env.test' not in f) else 0\" 2>/dev/null",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); print(f) if f.endswith('.py') else sys.exit(0)\" 2>/dev/null | xargs -I {} sh -c 'uv run ruff check --fix \"{}\" 2>/dev/null || true'",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
